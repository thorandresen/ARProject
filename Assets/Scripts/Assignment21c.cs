using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Assignment21c : MonoBehaviour
{
    [SerializeField] private GameObject Ox;
    [SerializeField] private GameObject As;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    bool i = false;
    // Update is called once per frame
    void Update()
    {
        //Vector3 targetDirection = Ox.transform.position - gameObject.transform.position;


        //Vector3 rotate = Vector3.RotateTowards(gameObject.transform.forward, targetDirection, 1.0f, 0.0f);
        //gameObject.transform.rotation = Quaternion.LookRotation(rotate);

   
        if (i)
        {
            //gameObject.transform.position = gameObject.transform.position + 
        } else
        {

        }
    }
}
